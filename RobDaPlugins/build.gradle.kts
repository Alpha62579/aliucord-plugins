version = "1.1.0"
description = "Get plugins from Robin.'s repo because they couldn't be bothered to put their repo on Aliucord."

aliucord.changelog.set(
        """
            # 1.0.0
            * Poof! The plugin now exists!
            # 1.1.0
            * Add links to GitHub profile and repository because why not?
        """.trimIndent()
)
