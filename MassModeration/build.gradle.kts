version = "1.0.2"

description = "Mass-ban/kick members."

aliucord.changelog.set(
        """
            # 1.0.0
            * Poof! The plugin now exists.
            # 1.0.1
            * Bug Fix: Make all error messages ephemeral.
            * Bug Fix: Fix permission check.
            # 1.0.2
            * Bug Fix: Dismiss Confirmation dialogs on OK.
            * Bug Fix: Clear banned/kicked members from list after mass-banning/kicking.
        """.trimIndent()
)
