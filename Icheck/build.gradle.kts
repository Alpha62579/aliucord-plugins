version = "1.0.1"
description = "Checks if the message starts with I and avert the crisis if it does not start with an I."

aliucord.changelog.set(
        """
            ## 1.0.1
            * Bug fix: Hardcode the default value into the actual logic instead of when creating the Settings.
        """.trimIndent()
)
